# Repository instructions

## 画像からのブラウザー表示一括確認

- マギアサークルの画像検証では、画像ファイルを直接入力して次を実行する。
  `npm run test:image-outputs -- <image-path>`
- このバッチは画像からOCRを行い、認識文字列を手入力せずに、相・紋・威力まで計算してJSONで出力する。
- 出力には呪文と語数、外円・内円、処理完了表示、相・紋それぞれの見出し／アイコン／全候補の割合、威力と内訳が含まれる。ブラウザー版の結果欄と詳細欄で表示する内容をまとめて確認できる。
- 相の説明文・割合、画像輪の共鳴率、威力計算はブラウザー版と同じ共有コードを使う。文章Embeddingにはブラウザー版と同じ Transformers.js WebAssembly backend、モデルID、q8設定を使う。
- 相の割合は類似度に対するsoftmax（temperature `0.04`）で差を広げ、最大剰余法で小数第1位に配分する。表示合計は100.0%で、順位に応じた係数は使わない。
- OCRの画像文字縮小はブラウザーCanvasに合わせたlinear補間を使い、画像から最後まで実行する。認識文字列を直接与えて結果を代用しない。
- 初回実行では文章Embeddingモデルの取得にネットワーク接続が必要。モデルは `Xenova/all-MiniLM-L6-v2` を使う。
- 画像OCRの確認だけを行う場合は `npm run test:spell-ocr -- <image-path>` を使う。
